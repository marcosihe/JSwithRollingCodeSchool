let numero = parseInt(prompt("Ingrese un número"));

if (numero % 5 == 0) {
    document.write("El número ingresado: " + numero + ", es divisible por 5");
} else {
    document.write("El número ingresado: " + numero + ", no es divisible por 5");
}